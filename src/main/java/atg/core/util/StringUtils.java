package atg.core.util;

public class StringUtils {

    public static String escapeHtmlString(String string){
        //TODO: Throwing exceptions in static methods would cause tests to fail
        //TODO: so need to decide what should we do in them?  kinda like constructors...
        //but harder to mock/override in unit tests
        return string;
    }
}
